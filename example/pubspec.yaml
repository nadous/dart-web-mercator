name: hello_mercator
description: Demoing the MercatorViewport on a Canvas

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_mercator:
    path : ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - json/martinique_poly.json